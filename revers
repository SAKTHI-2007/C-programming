#include<stdio.h>
void main()
{
    int num,r,t,sum=0;
    printf("input a number:");
    scanf("%d",&num);
    for(t=num;num!=0;num=num/10)
    {
        r=num%10;
        sum=sum*10+r;
    }
    printf("\nthe number in revers order is:%d",sum);
}
